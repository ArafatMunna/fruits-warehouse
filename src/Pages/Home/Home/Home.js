import React from 'react';
import Inventory from '../Inventory/Inventory';

const Home = () => {
    return (
        <div className="container">
            <Inventory />
        </div>
    );
};

export default Home;
